apply plugin: 'application'
apply plugin: 'java'

mainClassName = 'com.bushpath.anamnesis.namenode.Main'

// define repositories
repositories {
    mavenCentral()
}

// define dependencies
dependencies {
    compile project(':anamnesis')
}
